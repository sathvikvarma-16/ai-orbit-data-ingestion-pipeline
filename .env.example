# Copy this file to .env and fill in what you have.
# GITHUB_TOKEN is optional but strongly recommended (60 req/hr without it, 5000/hr with it).
GITHUB_TOKEN=
# Only needed for the "videos" module.
YOUTUBE_API_KEY=
# Only needed for LLM-generated descriptions (Step 3 of the task).
ANTHROPIC_API_KEY=
